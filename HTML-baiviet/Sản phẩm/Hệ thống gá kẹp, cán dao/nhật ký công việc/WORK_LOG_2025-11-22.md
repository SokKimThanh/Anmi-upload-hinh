WORK LOG — 2025-11-22

Mục tiêu hôm nay:
- Hoàn thiện chuyển logic tab từ inline script sang plugin.
- Đảm bảo ARIA + keyboard support và persistence chỉ trong session.
- Dọn sạch HTML (xóa inline script) và đảm bảo CSS/JS tập trung trong plugin.

Công việc đã làm:
- Cập nhật `plugins/plugins/js/tab-navigation.js`:
  - Hỗ trợ ARIA-based tabs (`.tab-buttons [role="tab"]` / `[role="tabpanel"]`).
  - Thêm điều khiển bàn phím: ArrowLeft/ArrowRight/Home/End/Enter/Space (cả Spacebar legacy).
  - Bổ sung fallback cho legacy selectors (`.tab-btn` / `.tab-content`).
  - Thêm chức năng restore tab từ `sessionStorage` (không lưu dài hạn).
  - Expose `window.AnmiTabNavigation` API: `init`, `saveActiveTab`, `activateTab`.

- Cập nhật persistence:
  - Chuyển từ `localStorage` → `sessionStorage` để không lưu lâu dài giữa các phiên.

- Chỉnh sửa HTML sản phẩm `17-hsk-gsk-ultra-precision-tool-holder.seo.html`:
  - Xóa inline tab `<script>` và thay bằng comment trỏ tới `plugins/plugins/js/tab-navigation.js`.
  - Giữ nguyên cấu trúc tab và các `id` panel: `overview`, `features`, `specifications`, `applications`, `contact`.

- Kiểm tra và chỉnh lý CSS (đã thực hiện trước đó):
  - Đã tập trung các rules tab vào `plugins/plugins/css/anmi-holder-products.css` (canonical `.tab-btn`, focus styles, biến CSS cho responsive).

- Cập nhật todo list nội bộ (đánh dấu các bước hoàn thành).

Kết quả:
- Tab logic đã được tập trung vào plugin, HTML trang sạch hơn, trải nghiệm keyboard/ARIA được cải thiện.
- Persistence chỉ tồn tại trong session (sessionStorage), phù hợp yêu cầu không lưu lâu dài.

Bước tiếp theo (đề xuất):
- Kiểm tra trên trang thật (mở DevTools → Network) để chắc `tab-navigation.js` được enqueue bởi plugin.
- Bump phiên bản plugin và deploy để force cache nếu cần.
- (Tuỳ chọn) Thêm `aria-live` để thông báo cho screen reader khi đổi tab.

Người thực hiện: GitHub Copilot (thay mặt dev trong workspace)
Thời gian: 2025-11-22
