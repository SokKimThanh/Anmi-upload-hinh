WORK LOG — 2025-11-22

Mục tiêu hôm nay:
- Hoàn thiện chuyển logic tab từ inline script sang plugin.
- Đảm bảo ARIA + keyboard support và persistence chỉ trong session.
- Dọn sạch HTML (xóa inline script) và đảm bảo CSS/JS tập trung trong plugin.

Công việc đã làm:
- Cập nhật `plugins/plugins/js/tab-navigation.js`:
  - Hỗ trợ ARIA-based tabs (`.tab-buttons [role="tab"]` / `[role="tabpanel"]`).
  - Thêm điều khiển bàn phím: ArrowLeft/ArrowRight/Home/End/Enter/Space (cả Spacebar legacy).
  - Bổ sung fallback cho legacy selectors (`.tab-btn` / `.tab-content`).
  - Thêm chức năng restore tab từ `sessionStorage` (không lưu dài hạn).
  - Expose `window.AnmiTabNavigation` API: `init`, `saveActiveTab`, `activateTab`.

- Cập nhật persistence:
  - Chuyển từ `localStorage` → `sessionStorage` để không lưu lâu dài giữa các phiên.

- Chỉnh sửa HTML sản phẩm `17-hsk-gsk-ultra-precision-tool-holder.seo.html`:
  - Xóa inline tab `<script>` và thay bằng comment trỏ tới `plugins/plugins/js/tab-navigation.js`.
  - Giữ nguyên cấu trúc tab và các `id` panel: `overview`, `features`, `specifications`, `applications`, `contact`.

- Kiểm tra và chỉnh lý CSS (đã thực hiện trước đó):
  - Đã tập trung các rules tab vào `plugins/plugins/css/anmi-holder-products.css` (canonical `.tab-btn`, focus styles, biến CSS cho responsive).

- Cập nhật todo list nội bộ (đánh dấu các bước hoàn thành).

Kết quả:
- Tab logic đã được tập trung vào plugin, HTML trang sạch hơn, trải nghiệm keyboard/ARIA được cải thiện.
- Persistence chỉ tồn tại trong session (sessionStorage), phù hợp yêu cầu không lưu lâu dài.

Bước tiếp theo (đề xuất):
- Kiểm tra trên trang thật (mở DevTools → Network) để chắc `tab-navigation.js` được enqueue bởi plugin.
- Bump phiên bản plugin và deploy để force cache nếu cần.
- (Tuỳ chọn) Thêm `aria-live` để thông báo cho screen reader khi đổi tab.

Người thực hiện: GitHub Copilot (thay mặt dev trong workspace)
Thời gian: 2025-11-22

---

Ghi chú bổ sung (cập nhật trong phiên làm việc):
- Thêm `.contact-info-wrapper` và `.contact-slider-dots` vào `17-hsk-gsk-ultra-precision-tool-holder.seo.html` để phù hợp với CSS hiện có và cho phép slider khởi tạo đúng.
- Cập nhật `plugins/plugins/js/contact-slider.js`:
  - Tạo dot navigation động nếu không có `.slider-dot` trong HTML.
  - Hoãn khởi tạo khi slider nằm trong tab ẩn bằng `MutationObserver`, đảm bảo layout/transform đúng khi tab hiển thị.
  - Bảo toàn hành vi trên desktop (không kích hoạt slider khi viewport > 768px).
- Cập nhật `plugins/plugins/css/anmi-holder-products.css` (mobile):
  - `.slider-dot` sửa để là hình tròn thực sự, đổi sang đơn vị `rem` (kích thước mặc định `0.625rem` ≈ 10px), điều chỉnh gap/padding/margin.
  - Thêm `overflow: hidden` cho `.contact-info-wrapper` trong mobile để tránh tràn nội dung.
- Cập nhật `plugins/plugins/js/tab-navigation.js`:
  - Khi focus panel/tab dùng `focus({ preventScroll: true })` với fallback lưu/restore scroll (tương thích IE11).
  - Tránh gọi `scrollToTabs()` mặc định; scroll chỉ xảy ra nếu mã khác chủ động gọi hàm này.

Vấn đề đã khắc phục:
- Ngăn trang nhảy/cuộn khi đổi tab bằng click hoặc keyboard.
- Đảm bảo contact slider khởi tạo đúng khi tab contact được bật (hoặc khi panel hiển thị).

Bước tiếp theo (đã/đề xuất):
- Kiểm tra trên thiết bị mobile thực tế xem slider hoạt động và dots hiển thị tròn.
- Nếu cần deterministic init, có thể gọi `initContactSlider()` trực tiếp từ `activateTab()` khi panel id là `contact` (mình có thể patch nhanh nếu bạn muốn).
- Bump plugin version và clear cache nếu trang không load file JS/CSS mới.
 
---

Latest edits (checked 2025-11-22):

- **Mobile tab list layout changed**: Updated `.tab-buttons` mobile rules to use a vertical/stacked scrollable area (`flex-direction: column` with `overflow-y: auto` and fixed height) to present more tabs per viewport; this was applied to improve spacing and allow larger pill buttons on small screens. Note: this changes earlier horizontal pill behavior; verify on devices and confirm this vertical layout is desired.

- **Hover scoping fixed**: Replaced `.tab-btn:hover` with `.tab-btn:not(.active):hover` so hover styles do not override an active tab's visual state.

- **Dot and slider CSS**: `.slider-dot` updated to `rem` units, forced circular aspect-ratio, and spacing adjusted (`gap`, `margin-top` in rem). `.contact-info-wrapper` kept `overflow:hidden` on mobile to avoid content overflow.

- **Tab focus/scroll JS**: `tab-navigation.js` uses `focus({preventScroll:true})` with fallback save/restore scroll position for browsers without `preventScroll`; this prevents automatic page scrolling when focusing tabs.

Action items / verification:
- Test on multiple mobile devices to confirm the vertical tab list UX is acceptable (or revert to horizontal pill with scroll-snap if you prefer horizontal navigation).
- If you want deterministic init of contact slider on tab activation, I can wire `initContactSlider()` from `activateTab()`.

Entry added by: GitHub Copilot (workspace edits)
Time: 2025-11-22
